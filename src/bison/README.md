Almost each rule map to one class and all its construct element as class member
Sometimes may use inheritance or sth else

val a: integer;
val b: boolean;
not allowed

instead:

val a: integer;
	b: boolean;

Parameters seperated by ; not ,

TODO:
add error recovery